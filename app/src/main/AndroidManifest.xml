<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <!-- Android TV Integration -->
    <uses-permission android:name="com.android.providers.tv.permission.WRITE_EPG_DATA" />

    <!-- Generic permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE"
        tools:ignore="LeanbackUsesWifi" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Search permissions (voice to text) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- The in-app updater hands a downloaded package to the system installer, the user has to
         allow this per app on Android 8+ -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Device feature requirements -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data
                android:host="youtube.com"
                android:scheme="https" />
        </intent>

        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="video/*" />
        </intent>

        <!-- Used by the in-app updater to detect the system package installer -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.android.package-archive" />
        </intent>
    </queries>

    <application
        android:name=".JellyfinApplication"
        android:allowBackup="true"
        android:appCategory="video"
        android:banner="@mipmap/app_banner"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_content"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jellyfin"
        android:usesCleartextTraffic="true">

        <!-- Screensaver -->
        <service
            android:name=".integration.dream.LibraryDreamService"
            android:exported="true"
            android:permission="android.permission.BIND_DREAM_SERVICE">

            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            android:initOrder="1000"
            tools:node="merge">
            <meta-data
                android:name="org.jellyfin.androidtv.LogInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="org.jellyfin.androidtv.di.KoinInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="org.jellyfin.androidtv.SessionInitializer"
                android:value="androidx.startup" />
        </provider>

        <provider
            android:name=".integration.MediaContentProvider"
            android:authorities="${applicationId}.content"
            android:exported="true"
            android:initOrder="10" />

        <provider
            android:name=".integration.provider.ImageProvider"
            android:authorities="${applicationId}.integration.provider.ImageProvider"
            android:exported="true"
            android:initOrder="10" />

        <!-- Shares a downloaded update package with the system package installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updates"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Removes the downloaded package once the new version replaced the old one -->
        <receiver
            android:name=".update.UpdatePackageReplacedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Authentication -->
        <activity
            android:name=".ui.startup.StartupActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity-alias
            android:name=".startup.StartupActivity"
            android:targetActivity=".ui.startup.StartupActivity" />

        <!-- Main application -->
        <activity
            android:name=".ui.browsing.MainActivity"
            android:configChanges="density|screenSize|smallestScreenSize|screenLayout|orientation|keyboard|keyboardHidden|navigation|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".ui.playback.ExternalPlayerActivity" />
    </application>
</manifest>
